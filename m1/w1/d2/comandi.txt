git init         //comando da lanciare per inizializzare una nuova repository

cd (nome cartella)     //entra nella cartella (avanza livello)

cd ../        //ritorna alla cartella di prima

git remote -v       //controlla se sei entrato nella repository

git status        //controlla se c'è roba da caricare (se non abbiamo aggiunto modifiche)

git add .        //aggiungo tutti i lavori

git add <file>         //agggiungi un dato lavoro

git commit -m "testo"       //assegna le modifiche che hai fatto

git config --global user.email
git config --global user.name

git log           //da direttamente le informazioni di accesso

git push origin master     //